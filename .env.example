# dbt-duckdb local development
# `dbt build` will create the file at this path; just edit if you want it elsewhere
DBT_DUCKDB_PATH=jaffle.duckdb

# Use the `ci` target instead of `dev` to write to an in-memory DuckDB
# (useful for repeatable runs without leftover state)
# DBT_TARGET=ci
